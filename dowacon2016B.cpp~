#include<cstdio>
#include<cstdlib>
#include<cmath>
#include<iostream>
#include<string>
#include<stack>
#include<queue>
#include<vector>
#include<map>
#include<set>
#include<algorithm>

#define rep(n) for(int i=0;i<n;i++)
#define repp(j, n) for(int j=0;j<n;j++)
#define reppp(i, m, n) for(int i=m;i<=n;i++)
#define all(c) c.begin(), c.end()
#define MOD 1000000007
#define MAX 1000000001
#define INF 1410065408
 
using namespace std;
typedef long long ll;
typedef pair<ll, ll> P;
struct edge{int from, to; ll cost;};

signed main(){
	string s;
	cin >> s;
	
	string s1(s), s2(s);
	rep((int)s.size()){
		if(s[i] == '?'){
			if(i % 2 == 0){
				s1[i] = '2';
				s2[i] = '5';
			}else{
				s1[i] = '5';
				s2[i] = '2';
			}
		}
	}
	
	int count1 = 0, count2 = 0, tmp1 = 0, tmp2 = 0;
	rep((int)s.size()){
		if(s1[i] == '2'){
		  
		}else if(s1[i] == '5' && tmp1 % 2 == 1){
		  tmp1++;
		}else{
		  count1 = max(tmp1, count1);
			tmp1 = 0;
		}
		/*
		if(s2[i] == '2'){
			tmp2++;
		}else if(s2[i] == '5' && tmp2 > 0){
			tmp2++;
		}else{
			count2 = max(tmp2, count2);
			tmp2 = 0;
		}
		*/

		printf("%c %d %c %d\n", (char)s1[i], tmp1, (char)s2[i], tmp2);
	}
	
	cout << count1;
}
