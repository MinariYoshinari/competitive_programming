#include<cstdio>
#include<cstdlib>
#include<cmath>
#include<iostream>
#include<string>
#include<stack>
#include<queue>
#include<vector>
#include<map>
#include<set>
#include<algorithm>

#define rep(n) for(int i=0;i<n;i++)
#define repp(j, n) for(int j=0;j<n;j++)
#define reppp(i, m, n) for(int i=m;i<=n;i++)
#define all(c) c.begin(), c.end()
#define MOD 1000000007
#define MAX 1000000001
#define INF 1410065408
 
using namespace std;
typedef long long ll;
typedef pair<ll, ll> P;
struct edge{int from, to; ll cost;};

signed main(){
  int  n;

  scanf("%d", &n);
  
  ll a[n];
  vector<int> ans;
  bool flag = true;
  rep(n){
	ll tmp;
	scanf("%lld", &tmp);
	if(i == 0){
	  a[0] = tmp;
	}else{
	  a[i] = a[i-1] + tmp;
	}
	if(a[i] != 0){
	  ans.push_back(i+1);
	  a[i] = 0;
	}else{
	  if(i == n-1) flag = false;
	}
  }

  if(flag){
	printf("YES\n");
	printf("%d\n", (int)ans.size());
	printf("%d %d\n", 1, ans[0]);
	rep((int)ans.size()-2){
	  printf("%d %d\n", ans[i]+1, ans[i+1]);
	}
	printf("%d %d", ans[ans.size()-2]+1, ans[ans.size()-1]);
  }else{
	printf("NO");
  }
}
